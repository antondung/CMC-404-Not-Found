{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npx --yes serve -s apps/web/dist -l tcp://0.0.0.0:${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
